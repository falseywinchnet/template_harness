\section{Introduction}

Define the object and problem in the vocabulary used by the principal result.
State that result early with domain, assumptions, quantifiers, and uncertainty
or strictness. Explain importance at the scale actually addressed, separate the
result's immediate inputs, position it against directly relevant prior work,
and give the argument in dependency order. Distinguish the target from famous
motivating conjectures and state boundaries that prevent predictable
misreadings. Road-map sentences should say what logical work a section performs.
